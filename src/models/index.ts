import { Message } from './Message';
import{ User} from './User';
import {Comment} from  "./Comment";
import {Chat} from "./Chat"
export default {
  user: new User(),
  message :new Message(),
  comment : new Comment(),
  chat : new Chat(),
};